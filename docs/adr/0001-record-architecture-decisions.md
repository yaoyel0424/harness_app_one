# ADR-0001：记录架构决策

## 状态

已采纳

## 背景

团队需要一个轻量但可扩展的方式来记录重要架构决策，避免知识流失。

## 决策

采用 **Architecture Decision Records (ADR)** 模式，存放于 `docs/adr/` 目录。

每份 ADR 包含：

- 标题与编号
- 状态（提议 / 已采纳 / 已废弃）
- 背景
- 决策
- 后果

## 后果

- 新成员可通过 ADR 了解历史决策上下文
- 变更架构时需新增 ADR 而非修改旧记录
